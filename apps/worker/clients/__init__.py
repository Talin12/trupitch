# Outbound HTTP clients the pipeline stages use: github.py (repo/tree/
# language lookups), llm.py (Stage 3 scoring), and errors.py (the shared
# RetryableError exception both raise on transient failures).
